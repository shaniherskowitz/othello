//
// Created by shani herskowitz on 12/17/17.
//

#include "EndGameCommand.h"
void EndGameCommand::execute(vector<string> args) {
  if (args.empty()) return;
  string gameName = args[0];

 // string gameToEnd = std::find(gamesList[0], gamesList.end(), args[0]);
/*  for (int i = 0; i < gamesList.size(); ++i) {
    if (gamesList[i].getName() == gameName) {
      gamesList[i].closeGame();
      return;
    }*/
  }
 // server->closeGame(args[0]);
}
